import { describe, expect, it } from 'vitest';

import { {{name}} } from './{{dashCase name}}';

describe('{{name}}', () => {
  // TODO: replace this with real assertions covering the edge cases the
  // implementation has to get right — empty input, Unicode, and boundaries.
  it('TODO: describe the behaviour under test', () => {
    expect({{name}}('')).toBe('');
  });
});
