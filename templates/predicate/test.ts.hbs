import { describe, expect, it } from 'vitest';

import { {{name}} } from './{{dashCase name}}';

describe('{{name}}', () => {
  // TODO: replace with real assertions — the true cases, the false cases, and
  // the edges for this input type.
  it('TODO: describe the behaviour under test', () => {
    expect({{name}}).toBeTypeOf('function');
  });
});
