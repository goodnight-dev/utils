/**
 * TODO: one-line summary of what {{name}} checks — this renders in the public
 * API docs, so make it good.
 *
 * @param value - TODO: describe the parameter.
 * @returns `true` when TODO, otherwise `false`.
 *
 * @example
 * ```ts
 * {{name}}('TODO') // => true
 * ```
 */
export function {{name}}(value: {{inputType}}): boolean {
  // TODO: implement {{name}}. Prefer a direct check to a regex where it is as
  // correct and faster (CONTRIBUTING.md §1).
  throw new Error('{{name}} is not implemented yet');
}

// ─── SCAFFOLD CHECKLIST — delete this whole block once every box is ticked ───
// These markers fail `pnpm lint` on purpose, so `pnpm check` stays red until the
// function is fully wired up. `pnpm new` already added the barrel export for you;
// the rest is yours:
//   TODO: implement {{name}} above (replace the `throw`).
//   TODO: cover the edge cases in ./{{dashCase name}}.test.ts.
//   TODO: list {{name}} in ./index.test.ts's expected public surface.
//   TODO: add {{name}} to the package README's API section.
//   TODO: run `pnpm changeset`.
// ─────────────────────────────────────────────────────────────────────────────
