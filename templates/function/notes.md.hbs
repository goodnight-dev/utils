# `{{name}}`

> TODO: one-paragraph summary of what `{{name}}` does and how it defines its
> edge cases (word boundaries, Unicode handling, empty input). This file is
> repo-only — it is never published to npm.

## Chosen implementation

TODO: paste the chosen implementation and explain _why_ this one — the
trade-offs that made it win over the alternatives below.

## Alternatives considered

TODO: document the approaches you rejected and why (performance, correctness,
readability). If you claim one is faster, back it with a benchmark — see
[Benchmarking a function](../../../docs/recipes/benchmarking-a-function.md), and
regenerate the harness any time with `pnpm new` (answer yes to the benchmark
prompt).

## Benchmarks

TODO: if this function makes a performance claim, drop the `pnpm bench` table
here with an environment stamp. Otherwise delete this section.

## Gotchas

TODO: anything a future reader (or you) might trip over — behaviour that looks
like a bug but is intentional, locale caveats, surrogate-pair handling, etc.
