/**
 * TODO: one-line summary of what {{name}} does — this renders in the public API
 * docs, so make it good.
 *
 * @param value - TODO: describe the parameter.
 * @returns TODO: describe the return value.
 *
 * @example
 * ```ts
 * {{name}}('TODO') // => 'TODO'
 * ```
 */
export function {{name}}(value: string): string {
  // TODO: implement {{name}}. Follow the house style in ./camel-case.ts — an
  // ASCII fast path, Unicode-correct handling on the cold path, and no regex
  // unless it is justified (see CONTRIBUTING.md §1).
  throw new Error('{{name}} is not implemented yet');
}

// ─── SCAFFOLD CHECKLIST — delete this whole block once every box is ticked ───
// These markers fail `pnpm lint` on purpose, so `pnpm check` stays red until the
// function is fully wired up. `pnpm new` already added the barrel export in
// ./index.ts for you; the rest is yours:
//   TODO: implement {{name}} above (replace the `throw`).
//   TODO: cover the edge cases in ./{{dashCase name}}.test.ts.
//   TODO: add {{name}} to the package README's API section.
//   TODO: run `pnpm changeset` (a new function is a minor bump).
// The entry-point test (./index.test.ts) is also failing right now: {{name}} is
// exported but missing from its expected-surface list — add it there too.
// ─────────────────────────────────────────────────────────────────────────────
